root = "."
tmp_dir = "tmp"

[build]
  cmd = "go build -o tmp\\main.exe .\\cmd\\api"
  bin = "tmp\\main.exe"
  delay = 1000
  include_ext = ["go", "tpl", "tmpl", "html"]
  exclude_dir = ["tmp", "bin", "docs", "scripts", "web"]

[log]
  time = true

[misc]
  clean_on_exit = true
